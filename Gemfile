source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in activerecord-has_one_defaults.gemspec
gemspec

gem 'activerecord', :require => 'activerecord'
